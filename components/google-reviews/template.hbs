{{!--
  Renders the Google Reviews block for the configured place. The visible markup +
  styling come from the plugin's `google_reviews` helper; this component just
  wires the editor's props (Place ID + count) to it.
--}}
{{ google_reviews(properties.place_id, properties.count) }}
